name: Docker Image CI

on:
  push:
    branches: [ bats ]
  pull_request:
    branches: [ bats ]

jobs:
     
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: textbook/git-checkout-submodule-action@master
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag debian-bats:$(date +%s)
